import { PizzaQueryFailedErrorFilter } from './pizza-query-failed-error.filter';

describe('PizzaQueryFailedErrorFilter', () => {
  it('should be defined', () => {
    expect(new PizzaQueryFailedErrorFilter()).toBeDefined();
  });
});
