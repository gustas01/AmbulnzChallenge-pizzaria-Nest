import { UserQueryFailedErrorFilter } from './user-query-failed-error.filter';

describe('UserQueryFailedErrorFilter', () => {
  it('should be defined', () => {
    expect(new UserQueryFailedErrorFilter()).toBeDefined();
  });
});
